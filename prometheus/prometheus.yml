global:
  scrape_interval: 10s

scrape_configs:
  - job_name: 'api'
    metrics_path: '/actuator/prometheus'
    scrape_interval: 5s
    static_configs:
      - targets: [
        'localhost:8090',
        'api:8090'
      ]
